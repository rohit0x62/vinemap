# Copy to ~/.config/vinemap/pypi-token (chmod 600) — never commit the real token.
# Or export PYPI_API_TOKEN in your shell / MCP config env block.
#
# Create token: https://pypi.org/manage/account/token/
# Scope: "Entire account" or project-scoped to vinemap

PYPI_API_TOKEN=pypi-AgEIcHlwaS5vcmcCJ...
